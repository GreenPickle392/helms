metrics:
  enabled: true       # aktiviert /metrics Endpoint
  serviceMonitor:
    enabled: true     # nur relevant, wenn Prometheus Operator vorhanden ist
    namespace: monitoring
    interval: 15s
    scrapeTimeout: 10s
    labels:
      release: prometheus
